# Setup at home

/dev/ttyACM0

lerobot-setup-motors \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0
    
lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=so101_follower_arm
    
/dev/ttyACM1

lerobot-setup-motors \
    --robot.type=so101_leader\
    --robot.port=/dev/ttyACM1


lerobot-calibrate \
    --teleop.type=so101_leader \
    --teleop.port=/dev/ttyACM1
    --teleop.id=so101_leader_arm

lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM1 \
    --robot.id=so101_leader_arm
    

lerobot-teleoperate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=so101_follower_arm \
    --robot.cameras="{front: {type: opencv, index_or_path: /dev/video2, width: 640, height: 480, fps: 30}, wrist: {type: opencv, index_or_path: /dev/video4, width: 640, height: 480, fps: 30}}" \
    --teleop.type=so101_leader \
    --teleop.port=/dev/ttyACM1 \
    --teleop.id=so101_leader_arm \
    --display_data=true
    

lerobot-teleoperate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=so101_follower_arm \
    --robot.cameras="{front: {type: opencv, index_or_path: /dev/video2, backend: V4L2, width: 640, height: 480, fps: 15}, wrist: {type: opencv, index_or_path: /dev/video4, backend: V4L2,  width: 640, height: 480, fps: 15}}" \
    --teleop.type=so101_leader \
    --teleop.port=/dev/ttyACM1 \
    --teleop.id=so101_leader_arm \
    --display_data=true



docker run -it --rm --gpus all --shm-size 5gb      -e SSH_PRIVATE_KEY="$(cat ~/.ssh/id_vast_lerobot | base64 -w 0)"      -e GIT_REPO_URL="git@github.com:DarioRepoRuler/lerobot.git"      -e GIT_BRANCH="cloud_feature"      Docker1Dario/lerobot-internal_cloud:latest


python -m lerobot.async_inference.robot_client \
    --server_address=85.10.218.46:36045 \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=so101_follower_arm \
    --robot.cameras="{front: {type: opencv, index_or_path: /dev/video2, backend: V4L2, width: 640, height: 480, fps: 15}, wrist: {type: opencv, index_or_path: /dev/video4, backend: V4L2, width: 640, height: 480, fps: 15}}" \
    --task="dummy" \
    --policy_type=pi0 \
    --pretrained_name_or_path=lerobot/pi0 \
    --policy_device=cuda \
    --actions_per_chunk=50 \
    --chunk_size_threshold=0.5 \
    --aggregate_fn_name=weighted_average \
    --debug_visualize_queue_size=True